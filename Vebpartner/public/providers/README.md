Provider logo assets live here.

Use stable local paths such as `/providers/highlevel.svg` when an authentic provider logo is supplied.
Until then, leave `provider.logo` empty so the UI falls back to provider text.
